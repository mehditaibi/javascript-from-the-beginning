const num1 = 100;
const num2 = 50;
let val;

// Simple math with numbers
val = num1 + num2;
val = num1 - num2;
val = num1 - num2;
val = num1 / num2;
val = num1 % num2;

// Math object
val = Math.PI;
val = Math.E;
val = Math.round(3.6);
val = Math.ceil(3.6);
val = Math.floor(3.6);
val = Math.sqrt(3.6);
val = Math.abs(-4);
val = Math.pow(3, 3);
val = Math.min(3, 4, 2, 5, 6);
val = Math.max(3, 4, 5, 1, 0, 8);
val = Math.ceil(Math.random() * 100); // number from 1 to 100

console.log(val);
